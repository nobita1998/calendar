<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Turtle TGE 第一次解锁（2025-10-22）— 抽奖活动</title>
  <link rel="icon" type="image/png" sizes="1024x1024" href="icon.png">
  <link rel="apple-touch-icon" href="icon.png">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: Arial, Helvetica, sans-serif;
      background: linear-gradient(135deg, #0c0c0f 0%, #1a1a23 100%);
      color: #fff;
      min-height: 100vh;
      padding: 20px;
    }
    .container {
      max-width: 820px;
      margin: 0 auto;
      background: rgba(23,23,31,.85);
      border: 1px solid #2b2b40;
      border-radius: 16px;
      padding: 28px;
      box-shadow: 0 20px 60px rgba(240,185,11,.10);
      backdrop-filter: blur(10px);
    }
    .header { display: flex; align-items: center; gap: 12px; margin-bottom: 24px; }
    .back-link {
      display: inline-block;
      background: linear-gradient(135deg,#0ecb81 0%,#1dd1a1 100%);
      color: #000; font-weight: 700; text-decoration: none;
      padding: 8px 12px; border-radius: 8px; outline: none;
      box-shadow: 0 2px 8px rgba(14,203,129,.3);
      transition: transform .2s ease, box-shadow .2s ease;
    }
    .back-link:hover, .back-link:focus { transform: translateY(-1px); box-shadow: 0 4px 16px rgba(14,203,129,.5); }
    .title {
      font-size: 1.9em; font-weight: 800; line-height: 1.2;
      background: linear-gradient(45deg,#f0b90b,#fcd535);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    }
    .subtitle { color: #848e9c; margin-top: 6px; }

    .card {
      background: linear-gradient(135deg, rgba(240,185,11,.10) 0%, rgba(23,23,31,.95) 100%);
      border: 1px solid #2b2b40; border-radius: 12px; padding: 20px; margin-top: 18px; position: relative;
    }
    .card::before { content: ""; position: absolute; left: 0; right: 0; top: 0; height: 3px; background: linear-gradient(90deg,#f0b90b,#fcd535); }
    .section-title { font-size: 1.2em; font-weight: 700; color: #fff; margin-bottom: 14px; }
    .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
    .stat { background: rgba(14,203,129,.06); border: 1px solid rgba(14,203,129,.25); border-radius: 10px; padding: 14px; text-align: center; }
    .label { color: #848e9c; font-size: .92em; margin-bottom: 6px; }
    .value { color: #0ecb81; font-size: 1.35em; font-weight: 800; }

    .reward-box { background: rgba(240,185,11,.10); border: 1px solid rgba(240,185,11,.35); border-radius: 10px; padding: 14px; margin-top: 14px; }
    .reward-row { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; }
    .reward-row:last-child { margin-bottom: 0; }
    .reward-label { color: #848e9c; }
    .reward-value { color: #f0b90b; font-weight: 800; }

    .actions { margin-top: 16px; display: flex; gap: 10px; flex-wrap: wrap; align-items: center; }
    .btn {
      background: linear-gradient(135deg,#f0b90b,#fcd535);
      color: #000; font-weight: 800; padding: 10px 14px; border-radius: 10px; border: none; cursor: pointer;
      box-shadow: 0 2px 8px rgba(240,185,11,.35); outline: none;
    }
    .btn:hover, .btn:focus { transform: translateY(-1px); box-shadow: 0 4px 16px rgba(240,185,11,.55); }
    .note { color: #848e9c; font-size: .9em; margin-top: 6px; }

    .result { margin-top: 14px; padding: 12px; border-radius: 10px; background: rgba(23,23,31,.6); border: 1px solid #2b2b40; }
    .result-strong { color: #26a69a; font-weight: 800; }

    .watermark { text-align: right; color: #848e9c; margin-top: 20px; }

    @media (max-width: 768px) {
      body { padding: 12px; }
      .container { padding: 20px; }
      .grid { grid-template-columns: 1fr; }
      .title { font-size: 1.5em; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <a href="index.html" class="back-link" aria-label="返回首页"
         onclick="if (document.referrer && new URL(document.referrer).origin === location.origin) { history.back(); return false; }">返回</a>
      <div>
        <h1 class="title">Turtle TGE 第一次解锁</h1>
        <div class="subtitle">日期：2025年10月22日 · 仅包含抽奖内容</div>
      </div>
    </div>

    <div class="card" id="lottery">
      <div class="section-title">抽奖概览（第一次解锁）</div>
      <div class="grid">
        <div class="stat">
          <div class="label">参与人数</div>
          <div class="value" id="participantsText">50,000</div>
        </div>
        <div class="stat">
          <div class="label">中奖率</div>
          <div class="value" id="winRateText">≈ 33.33%</div>
        </div>
        <div class="stat">
          <div class="label">预期中奖人数</div>
          <div class="value" id="expectedWinners">~ 16,667</div>
        </div>
        <div class="stat">
          <div class="label">单人奖励</div>
          <div class="value">22 TURTLE</div>
        </div>
      </div>

      <div class="reward-box">
        <div class="reward-row">
          <span class="reward-label">预期发放总量</span>
          <span class="reward-value" id="expectedTotal">~ 366,674 TURTLE</span>
        </div>
      </div>

      <div class="actions">
        <button class="btn" id="simulateBtn" aria-controls="simResult">模拟一次抽奖</button>
        <span class="note">说明：一次性从 50,000 名参与者中独立抽取，命中概率 1/3。</span>
      </div>
      <div class="result" id="simResult" aria-live="polite"></div>
    </div>

    <div class="watermark">X: @NNNNNNOBITA</div>
  </div>

  <script>
    // Constants
    const participants = 50000;
    const winRate = 1 / 3; // ≈33.33%
    const rewardPerWinner = 22; // TURTLE

    // Helpers
    const fmt = (n) => Number(n).toLocaleString('zh-CN');

    // Initialize derived values
    const expectedWinners = Math.round(participants * winRate);
    const expectedTokens = expectedWinners * rewardPerWinner;
    document.getElementById('expectedWinners').textContent = `~ ${fmt(expectedWinners)}`;
    document.getElementById('expectedTotal').textContent = `~ ${fmt(expectedTokens)} TURTLE`;

    // Simulation (simple independent Bernoulli trials)
    function simulateOnce() {
      let wins = 0;
      for (let i = 0; i < participants; i++) {
        if (Math.random() < winRate) wins++;
      }
      const total = wins * rewardPerWinner;
      return { wins, total };
    }

    const simBtn = document.getElementById('simulateBtn');
    const simResult = document.getElementById('simResult');
    simBtn.addEventListener('click', () => {
      const { wins, total } = simulateOnce();
      simResult.innerHTML = `
        <div>本次模拟中奖人数：<span class="result-strong">${fmt(wins)}</span></div>
        <div>本次模拟发放总量：<span class="result-strong">${fmt(total)} TURTLE</span></div>
        <div class="note">提示：结果会随随机性轻微波动，长期期望约为 ${fmt(expectedWinners)} / ${fmt(expectedTokens)}。</div>
      `;
    });
  </script>
  <script src="scripts/analytics.js" defer></script>
</body>
</html>

